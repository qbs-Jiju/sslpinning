package com.sslpinning

import com.facebook.react.bridge.ReactApplicationContext

abstract class SslpinningSpec internal constructor(context: ReactApplicationContext) :
  NativeSslpinningSpec(context) {
}
